# Dependencies node_modules/ npm-debug.log* yarn-debug.log* yarn-error.log* #
Runtime data pids *.pid *.seed *.pid.lock # Coverage directory used by tools
like istanbul coverage/ *.lcov # nyc test coverage .nyc_output # Environment
variables .env .env.local .env.development.local .env.test.local
.env.production.local # Build outputs dist/ build/ # Vite (if using supernal-web
profile) # .vite/ # IDEs .vscode/ .idea/ # OS .DS_Store Thumbs.db # Docker (if
using docker tools) # .dockerignore # TypeScript (if using typescript tools) #
*.tsbuildinfo # Testing (if using jest tools) # coverage/ # Logs logs *.log #
Temporary files tmp/ temp/ # Supernal Coding .supernal-code/ .supernal-command/