const { TemplateLoader } = require('./loader');
const { DocumentRegistry } = require('./registry');

export {
  TemplateLoader,
  DocumentRegistry
};

module.exports = {
  TemplateLoader,
  DocumentRegistry
};
